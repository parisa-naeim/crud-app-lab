
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/style.css">
    <title>Foods Index</title>
  </head>
  <body>
    <h1><%= food.name%></h1>
    
        <p>Description:<%= food.description%></p>
        <p>Recipe: <%= food.recipe%></p>
        <p>Origin: <%= food.origin%></p>
        <p>
          <% if(food.isVegetarian) {%>
            It is a vegetarian food.
          <% } else { %>
            It is not a vegetarian food.
            <% } %>
        </p> 

        <form action="/foods/<%=food._id%>?_method=DELETE" method="POST">
            <button type="submit" class="button">Delete <%= food.name %></button>
            <a class="button" href="/foods/<%= food._id %>/edit">Edit <%= food.name %></a>
        </form>
  </body>
</html>
